{
  "name": "la-project",
  "version": "1.0.0",
  "description": "Real estate project",
  "scripts": {
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  }
}
